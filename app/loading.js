import Splash from "../components/Splash";

/* Le repli de toute navigation : Next l'affiche pendant qu'un composant
   serveur travaille. */
export default function Loading() {
  return <Splash />;
}
